# Expose retrieval evidence traces

The initial interface will provide a collapsible retrieval-evidence view showing
whether each candidate came from vector, keyword, or graph retrieval, the graph
path used, reranking position, exact source passage, and claim-verification
result. The main cited answer remains concise, while the trace makes retrieval
behavior and failures inspectable.


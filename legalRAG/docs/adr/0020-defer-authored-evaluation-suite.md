# Defer the authored evaluation suite

The initial implementation will not require a manually authored regression and
quality-evaluation dataset. Development will proceed using runtime provenance,
citation validation, and exploratory testing, while a reviewed evaluation suite
and formal release thresholds are deferred until the core pipeline is working.


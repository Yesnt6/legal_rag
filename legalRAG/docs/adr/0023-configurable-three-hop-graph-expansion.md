# Default graph expansion to three configurable hops

Graph-assisted retrieval will begin from entities and concepts identified in the
question and traverse at most three relationships by default. The hop limit will
be configurable for later tuning, while allowed relationship types, candidate
caps, reranking, and source-passage verification constrain noise and latency.

